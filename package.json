{
  "name": "my-app",
  "version": "0.1.0",
  "description": "My App!",
  "main": "main.js",
  "scripts": {
    "build": "gulp build",
    "build-pro": "gulp build --pro",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webix"
  ],
  "devDependencies": {},
  "dependencies": {
    "@types/electron": "^1.4.37",
    "@types/node": "^7.0.18",
    "@types/webix": "^4.2.0"
  }
}
